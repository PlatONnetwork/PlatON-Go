package common

/*func MustToString(obj interface{}) string {
	if obj == nil {
		return ""
	}
	bs, err := json.Marshal(obj)
	log.Error("cannot marshal object", "err", err)
	return string(bs)
}

func MustToObject(bs []byte, objRefer *interface{}) {
	if len(bs) == 0 {
		return
	}
	err := json.Unmarshal(bs, objRefer)
	log.Error("cannot unmarshal to object", "err", err)
}*/
